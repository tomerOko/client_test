import styled from 'styled-components/macro';
import { AbstractButton } from '../../abstract/AbstractButton';
import { mainColors } from '..';

export const MainButton = styled(AbstractButton)`
  ${mainColors}
`;
